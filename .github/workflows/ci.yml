name: CI Workflow

on: push

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    
    - name: Checkout code
      uses: actions/checkout@v4

    
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '22'  
        cache: 'npm'        

    
    - name: Install dependencies
      run: npm ci

    
    - name: Lint the code
      run: npm run lint

    
    - name: Check code formatting
      run: npm run format -- --check

    
    - name: Build the project
      run: npm run build

    
    - name: Run development environment
      run: npm run dev
      if: github.event_name == 'pull_request' 

    
    - name: Start the app
      run: npm run start
      if: github.event_name == 'pull_request' 

---
Notify final testing status ---
