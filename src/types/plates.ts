// not all attributes can exist in all scrapers so :

export type Plate = {
  link?: string;
  img?: string;
  duration?: string;
  price?: string;
  emirate?: string;
  character?: string;
  number?: string;
  contact?: string;
  source: string;
};
